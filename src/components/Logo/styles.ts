import { StyleSheet } from "react-native"

export const createStyles = () => {

  return StyleSheet.create({
    container: {},
    image: {
      height: 180,
      resizeMode: 'contain'
    }
  })
}